version: 2

build:
  image: 

python:
  version: "3.8"
  install:
    - method: pip
      path: .
      extra_requirements:
        - docs

build:
  os: ubuntu-20.04
  tools:
    python: "3.8"

sphinx:
  configuration: docs/conf.py